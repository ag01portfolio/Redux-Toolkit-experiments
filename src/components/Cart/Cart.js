import Card from "../UI/Card";
import classes from "./Cart.module.css";
import CartItem from "./CartItem";

const Cart = (props) => {
  return <Card className={classes.cart}></Card>;
};

export default Cart;
